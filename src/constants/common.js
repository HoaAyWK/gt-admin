export const PAGE_SIZES = Object.freeze({
  "DEFAULT": 10,
  "MEDIUM": 25,
  "LARGE": 50
});

export const ORDER_BY = Object.freeze({
  "ASC": "asc",
  "DESC": "desc"
});

export const ORDER_PER_PAGE_OPTIONS = Object.freeze([5, 10, 25, 50, 100]);
